# A mask is a blind spot you chose. Adding one is a review, not a quick fix.
/normalise/masks.yaml  @tutors-sdk/maintainers

# The harness's own signal: changing what a mutant plants or what it must be
# attributed to changes what the gate can promise.
/mutants/              @tutors-sdk/maintainers

# The verdict and the exit code: what may fail a release, and when it may not.
/src/gate.ts           @tutors-sdk/maintainers

# The integration contract the monorepo builds against, its machine-readable
# half, and the version constants it is stamped from. A change here is a
# contract version bump (docs/contract.md, "Compatibility").
/docs/contract.md      @tutors-sdk/maintainers
/docs/contract/        @tutors-sdk/maintainers
/src/version.ts        @tutors-sdk/maintainers

# The guard that keeps masks in their own PR, and the ratchet that says when the
# harness has earned the right to fail: weakening either weakens the gate.
/src/ci/mask-change.ts   @tutors-sdk/maintainers
/src/ci/noise-history.ts @tutors-sdk/maintainers
/docs/noise-burndown.md  @tutors-sdk/maintainers
